generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  ADMIN
  OWNER
  CUSTOMER
}

model User {
  id              String        @id @default(cuid())
  phoneE164       String        @unique
  name            String?
  email           String?       @unique
  password        String
  role            UserRole      @default(CUSTOMER)
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  phoneVerifiedAt DateTime?

  // علاقة المالك مع المطاعم
  restaurants     Restaurant[]

  // علاقة المستخدم مع الطلبات (الزبون)
  orders          Order[]
}

model Restaurant {
  id          String       @id @default(cuid())
  name        String
  description String?
  logo        String?

  ownerId     String
  owner       User         @relation(fields: [ownerId], references: [id])

  // علاقة المطعم مع المنتجات
  products    Product[]

  // علاقة المطعم مع الطلبات
  orders      Order[]

  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

model Product {
  id           String       @id @default(cuid())
  name         String
  price        Float
  description  String?
  image        String?
  
  restaurantId String
  restaurant   Restaurant   @relation(fields: [restaurantId], references: [id])

  // علاقة المنتج مع العناصر داخل الطلب
  orderItems   OrderItem[]

  createdAt    DateTime     @default(now())
}

enum OrderStatus {
  PENDING
  PREPARING
  DELIVERED
  CANCELLED
}

model Order {
  id           String       @id @default(cuid())
  userId       String
  restaurantId String
  totalPrice   Float
  status       OrderStatus  @default(PENDING)
  createdAt    DateTime     @default(now())

  // العلاقات
  user        User          @relation(fields: [userId], references: [id])
  restaurant  Restaurant    @relation(fields: [restaurantId], references: [id])
  items       OrderItem[]
}

model OrderItem {
  id        String      @id @default(cuid())
  orderId   String
  productId String
  quantity  Int         @default(1)

  // العلاقات
  order     Order       @relation(fields: [orderId], references: [id])
  product   Product     @relation(fields: [productId], references: [id])
}
